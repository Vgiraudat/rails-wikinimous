<div class="container">

  <h1>ARTICLES</h1>

  <% @articles.each do |article| %>
    <ul>
      <li>
        <%= link_to article.title, article_path(article) %>
        <%= article.content %>
      </li>
    </ul>
  <% end %>

  <%= link_to "New article", new_path %>

</div>
